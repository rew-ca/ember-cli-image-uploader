import {
  imageTransform
} from '../../../helpers/image-transform';
import { module, test } from 'qunit';

module('ImageTransformHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = imageTransform(42);
  assert.ok(result);
});
